version: "3.9"
services:
  backend:
    build: ./backend
    ports:
      - "127.0.0.1:8011:8011"
    volumes:
      - ./backend/data:/app/data
    environment:
      - ENV=production

  frontend:
    build: ./frontend
    ports:
      - "127.0.0.1:5173:80"
    depends_on:
      - backend
